# canvas
## 基本语法
1. 设置宽高
css设置：的是元素本身的大小，当元素大小与绘图表面大小不相符时，浏览器会缩放，易导致出现奇怪的效果
设置属性：是同时修改元素本身和元素绘图表面的大小
2. 三个方法
1）getContext
2）toDataURL(type,quality) 返回img src 的地址
3）toBlob(callback,type,args...)
## canvas游戏
1. 离屏
2. 上屏