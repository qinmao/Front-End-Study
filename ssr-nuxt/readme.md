# ssr 
## 使用的场景,问题
* 场景
    1. 更好的 SEO，由于搜索引擎爬虫抓取工具可以直接查看完全渲染的页面
    2. 更快的内容到达时间 (time-to-content)，特别是对于缓慢的网络情况或运行缓慢的设备
* 问题
    1. 要处在nodejs.serve 运行环境
    2. 更多的服务器负载

## nuxt
> 注意：使用脚手架创建项目时，箭头选择不能再bash 下，使用windows 默认的cmd 环境